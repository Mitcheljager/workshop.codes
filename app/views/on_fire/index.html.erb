<% content_for :page_title, "On Fire #{ "| Page #{ params[:page] }" if params[:page] }" %>

<div class="heading">
  <h1>
    <%= inline_svg_tag "icons/icon-onfire.svg", class: "title-icon" %>
    <strong>On Fire</strong> Workshop Codes <%= tag.small "- Page #{ params[:page] }" if params[:page] %>
  </h1>
</div>

<%= render "posts/posts" %>
