<div class="modal" data-modal="report">
  <div class="modal__backdrop" data-role="modal-backdrop" role="dialog" aria-labelledby="report"></div>

  <div class="modal__content">
    <h2 class="modal__title" id="report">Report <%= params[:concerns_model] %> <strong><%= @post.code if @post.present? %></strong></h2>

    <%= render "form_#{ params[:concerns_model] }" if params[:concerns_model] %>
  </div>
</div>
