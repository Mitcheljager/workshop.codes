<% content_for :page_title, t("users.new.title") %>

<center>
  <h1 class="mb-1/2 mt-1/1"><%= t("users.new.title") %></h1>
</center>

<div class="wrapper--small">
  <div class="well well--dark">
    <%= render "form", user: @user, url: users_path(elohell: params[:elohell]) %>
  </div>

  <p><center><small><%= link_to t("users.new.login"), login_path %></small></center></p>
</div>
