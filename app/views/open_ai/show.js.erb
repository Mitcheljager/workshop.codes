(() => {
  const element = document.querySelector('[data-role="ai-prompt"]')
  const messageElement = element.querySelector('[data-role="ai-message"]')
  const form = element.querySelector("form")

  form.remove()
  messageElement.innerHTML = `
    <%= @message %>
    <p class="text-dark text-small"><em>This is a beta feature and the results may or may not be correct. I'm still trying to get better at understanding the Workshop, just like you!</em></p>
    <p class="text-dark text-small"><em> This message was generated by Snowball, totally not by Null Sector.</em></p>
  `
})()
