<% content_for :page_title, "#{ t("navigation.on_fire") } #{ "| Page #{ params[:page] }" if params[:page] }" %>

<div class="heading">
  <h1>
    <%= inline_svg_tag "icons/icon-onfire.svg", class: "title-icon" %>
    <strong><%= t("navigation.on_fire") %></strong>
    Workshop Codes <%= tag.small "- Page #{ params[:page] }" if params[:page] %>
  </h1>
</div>

<main class="items">
  <%= render "posts" %>
</main>
