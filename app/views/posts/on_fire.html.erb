<% content_for :page_title, "On Fire Items #{ "| Page #{ params[:page] }" if params[:page] }" %>

<div class="heading items-heading">
  <h1><strong>On Fire</strong> Workshop Codes <%= tag.em "- Page #{ params[:page] }" if params[:page] %></h1>

  <%= render "profile_block" %>
</div>

<main class="items">
  <%= render "posts" %>

  <%= paginate @posts %>
</main>
