<section class="revisions">
  <div class="items-heading mt-0 mb-0">
    <h2 class="mt-0">Update Log <small>(<%= @revisions.size %>)</small></h2>
  </div>

  <%= render collection: @revisions, partial: "revisions/revision", as: :revision %>
</section>
