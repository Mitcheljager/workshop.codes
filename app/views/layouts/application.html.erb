<!DOCTYPE html>
<html>
  <head>
    <title><%= yield(:page_title) != "" ? yield(:page_title) + " | Workshop.codes" : "Workshop.codes - Overwatch Workshop Codes" %></title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="description" content="Workshop.codes">
    <meta name="keywords" content="Workshop.codes">
    <meta name="theme-color" content="#fa9901">

    <link rel="icon" type="image/png" href="<%= image_path "favicon/favicon-colored-32x32.png" %>" sizes="32x32" />
    <link rel="icon" type="image/png" href="<%= image_path "favicon/favicon-colored-16x16.png" %>" sizes="16x16" />

    <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbolinks-track": "reload" %>
  </head>

  <body>
    <div class="wrapper">
      <%= render "navigation" %>

      <%= yield %>

      <%= render "footer" %>
    </div>

    <%= render "body_bg" %>
  </body>
</html>
