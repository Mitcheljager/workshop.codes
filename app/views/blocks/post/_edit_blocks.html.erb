<div>
  <div data-role="blocks block-sortable">
    <% @post.blocks.each do |block| %>
      <%= render "blocks/post/block_settings", block: block %>
    <% end %>
  </div>

  <h2 class="mt-1/1">Create blocks</h2>

  <p class="form-hint">
    <mark>Blocks allow you to customise the landingpage of your profile.</mark> Add blocks to your profile to highlight particular codes or other aspects you want to show off. <strong>You can add a maximum of 3 blocks.</strong><br>
    <em class="block text-dark"><small>Reorder blocks by dragging them</small></em>
  </p>

  <%= render "blocks/post/add_blocks" %>
</div>
