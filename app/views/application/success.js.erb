(() => {
  const element = document.createElement("div")
  element.classList.add("alert")
  element.innerText = "Successfully saved"

  document.body.prepend(element)

  setTimeout(() => {
    element.remove()
  }, 5000)
})()
