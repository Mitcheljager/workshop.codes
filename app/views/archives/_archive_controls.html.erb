<div class="form-group-columns">
  <div class="well well--dark">
    <p class="mt-0">You may choose to transfer this post to yourself.</p>

    <p>Transferring this post will make you the owner of this post. Once the post is transferred, the post will no longer be marked as an archive post and you will be able to manage the post as if it were a normal post.</p>

    <%= link_to archive_path(code: post.code), method: :patch, class: "button w-100 mt-1/4", data: { prefetch: false, confirm: "Are you sure you want to transfer this post to your account? \n\n This will transfer archive post: \n #{ post.code } \n\n Title: \n #{ post.title } \n\n to your account." } do %>
      <%= t "archive.navigation.transfer" %>
    <% end %>
  </div>
  <div class="form-group mt-1/4 sm:mt-0">
    <div class="well well--dark">
      <p class="mt-0">You may also choose to delete this post.</p>

      <p class="text-orange">Deleting this post is <b>permanent and cannot be undone</b>.</p>

      <%= link_to archive_path(code: post.code), method: :destroy, class: "button button--danger w-100 mt-1/4", data: { prefetch: false, confirm: "Are you sure you want to delete this post? This cannot be undone. \n\n This will delete archive post: \n #{ post.code } \n\n Title: \n #{ post.title }" } do %>
        <%= t "archive.navigation.delete" %>
      <% end %>
    </div>
  </div>
</div>
